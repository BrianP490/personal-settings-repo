# PERSONAL GLOBAL CONFIGURATIONS I USE

For `pyproject.toml` 

1. (Windows): Create and store in ~\\AppData\\Roaming\ruff

---

`settings.json` Settings to customize: 

1. `python.defaultInterpreterPath`: The absolute path of the python.exe installed on your system.

2. `ruff.configuration`: The absolute path of where the `pyproject.toml` file is stored.